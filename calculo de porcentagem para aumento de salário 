#include <stdio.h>

int main() {
    char nome[30];
    int salario;
    printf("meu nome: ");
    scanf("%s", &nome[30]);
    printf("meu salario: ");
    scanf("%d", &salario);
    salario += salario * 0.15;
    printf("o salário de %s agora é: %d",nome,salario);
    
    return 0;
}