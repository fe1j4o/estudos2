#include <stdio.h>

int main() {
    int num;
    printf("digite um numero: ");
    scanf("%d", &num);
    num %= 2;
    if(num==0){
        printf("\neste numero é par");
    }
    else{
        printf("\neste número é ímpar ");
    }

    return 0;
}