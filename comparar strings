#include <stdio.h>
#include <string.h>
int main() {
    char senha[20];
    printf("digite a senha: ");
    scanf("%s", senha);
    if(strcmp(senha,"1234")== 0){
        printf("senha correa");
    }
    else{
        printf("senha incorreta");
    }

    return 0;
}