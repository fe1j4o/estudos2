#include <stdio.h>
#include <math.h>

int main() {
    float paes, broas, total; 
    printf("digite a quantidade de pães vendidos: ");
    scanf("%f", &paes);
    printf("\n digite a quantidade de broas vendidas: ");
    scanf("%f", &broas);
    paes *= 0.12 ;
    broas *= 1.50;
    //total = (10 / 100) * total;
    printf("%.2f %.2f", paes, broas);
    printf("\n %f", total);
    

    return 0;
}
